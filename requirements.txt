# No runtime deps for rank.py / src/ on purpose (stdlib only: json, gzip, csv,
# re, math, dataclasses) -- one less thing to break in the grading sandbox.
# Needs Python 3.10+.
#
# dev/test only:
pytest==8.3.4
